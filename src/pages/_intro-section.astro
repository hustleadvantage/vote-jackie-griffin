---
import { Image } from '@astrojs/image/components';
import { Container, Icon, Link, Text } from '../components';
import { ArrowRight } from 'svelte-heros-v2';
import { HomeIntroImage } from '../assets/home';
---

<div class='py-8'>
  <Container>
    <div
      class='flex flex-col md:flex-row-reverse items-center gap-8 bg-white rounded-lg overflow-hidden'
    >
      <div class='w-full md:w-1/2'>
        <Image
          class='rounded-lg'
          alt='hero'
          src={HomeIntroImage}
          width={1824}
          height={2736}
        />
      </div>

      <div class='w-full md:w-auto'>
        <div class='grid gap-5 p-6'>
          <h2>Jacqueline’s Story.</h2>
          <div class='grid gap-4'>
            <Text>
              As a mom, graduate, and resident in the city of Rochester,
              Jacqueline understands firsthand the importance of quality
              education.
            </Text>
          </div>
          <Link href='/about'>
            Meet Jacqueline
            <Icon icon={ArrowRight} size='20' />
          </Link>
        </div>
      </div>
    </div>
  </Container>
</div>
